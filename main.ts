// src/main.ts

// Example: TypeScript function to generate a greeting
export function greet(name: string): string {
    return `Hello, ${name}! Welcome to Wi-Fi QR Code Maker.`;
}

// Example usage (for demonstration)
console.log(greet('User')); 